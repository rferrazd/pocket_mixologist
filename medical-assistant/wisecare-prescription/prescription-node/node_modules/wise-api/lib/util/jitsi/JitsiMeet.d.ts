import StartConferenceOptions from './interfaces/StartConferenceOptions';
declare class JitsiMeet {
    private JitsiExternalApi;
    private timeAlarm;
    private options;
    private opts;
    startConference(opts: StartConferenceOptions): Promise<void>;
    private onLoad;
    private onClose;
    closeConference: (forEveryone?: boolean) => void;
    getNumberOfPart(): number | undefined;
    executeCommand(command: string, data?: any): void;
}
export default JitsiMeet;
