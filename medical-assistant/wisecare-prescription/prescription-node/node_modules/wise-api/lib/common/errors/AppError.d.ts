declare class AppError extends Error {
    message: string;
    protected code: number;
    protected internal: boolean;
    constructor(message: string, code?: number, internal?: boolean);
}
export default AppError;
