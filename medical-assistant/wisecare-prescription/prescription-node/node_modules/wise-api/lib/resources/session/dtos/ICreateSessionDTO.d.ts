interface ICreateSessionDTO {
    org: string;
    orgUnit: string;
    service?: string;
    profile?: string;
    skin?: string;
    owner?: string | null;
    joinPolicy?: ('ORG' | 'ORGUNIT' | 'RESTRICT' | 'PUBLIC' | 'JOINPWD');
    listPolicy?: ('PUBLIC' | 'SHARED' | 'PRIVATE');
    allowJoinOu?: Array<number>;
    allowTranscription?: boolean;
    allowRegistryDLT?: boolean;
    logo?: string;
    joinPwd?: string;
    allowParts?: Array<{
        user: number;
        role?: ('ADMIN' | 'OPEN' | 'GUEST' | 'HOST')[];
    }>;
}
export default ICreateSessionDTO;
