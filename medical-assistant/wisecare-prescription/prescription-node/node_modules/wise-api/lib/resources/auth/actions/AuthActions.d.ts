import ILoginDTO from '../dto/ILoginDTO';
import ILoginResponse from '../responses/ILoginResponse';
declare class Auth {
    private url;
    private request;
    constructor(url?: string);
    loginByOrg(data: ILoginDTO): Promise<ILoginResponse>;
    loginByUser(data: ILoginDTO): Promise<ILoginResponse>;
    loginBySystemClient(data: ILoginDTO): Promise<ILoginResponse>;
}
export default Auth;
